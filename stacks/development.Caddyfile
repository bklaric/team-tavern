# The prerenderer resolves `localhost` to its own container, so the site also
# answers to its compose service name over plain HTTP, and that name is the
# render origin.

localhost, http://caddy {
    import base.Caddyfile http://caddy
}
