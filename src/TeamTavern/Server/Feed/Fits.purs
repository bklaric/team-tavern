module TeamTavern.Server.Feed.Fits (fitsText) where

-- | The posts one post fits, as `Fits.sql` describes it: a select, for the
-- | query that notifies their owners to wrap.
foreign import fitsText :: String
